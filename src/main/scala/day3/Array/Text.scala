package day3.Array

import java.util
import scala.collection.JavaConversions._
object Text {
  def main(args: Array[String]): Unit = {
    val b=new util.HashSet[String]();

    for (x<- b){
      print(x)
    }

    val a=new util.ArrayList[Int]

    print(a(1))
  val s=a.iterator();
    while (s.hasNext()){
     print(s.next())
    }
//// 一定要导入import scala.collection.JavaConversions._
//    for (x<-a){
//      print(x)
//    }






  }
}
